#include<iostream>
#include<string>
#include<vector>

using namespace std;

int main()
{
  vector<char> code, guess, hints(4,0);
  char n;
  
  code.reserve(4); guess.reserve(4);
  
  cout<<"Enter the matrix\nr - red\nblue\ngreen\nyellow\nviolet\norange";
  for(int i=0;i<4;)
   {cin>>n;
    if(n=='r' || n=='b' || n=='g' || n=='y' || n=='v' || n=='o')
        {code.push_back(n);i++;}
    else cout<<"Try again";
   }
   
    for(int i=0;i<10;)
   {cin>>n;
    if(n=='r' || n=='b' || n=='g' || n=='y' || n=='v' || n=='o')
        {guess.push_back(n);i++;}
    else cout<<"Try again";
   
   if
   
   }
   
}




#include<iostream>
#include<string>
#include<vector>

using namespace std;

int main()
{
  vector<char> code = {'r','r','r','r'} , guess, hints;
  char c; bool result=false; int m=0, n=0;
  
  code.reserve(4); guess.reserve(4), hints.reserve(4);
  
  while(result==false )     //|| n<10
 {  
  cin>>result;
    }
guess.clear();
n++;
 
 
   
   
   
}




/*
cout<<"Enter the matrix\nr - red\nblue\ngreen\nyellow\nviolet\norange";
 
  for(int i=0;i<4;)
   { 
        cin>>c;
        if(c=='r' || c=='b' || c=='g' || c=='y' || c=='v' || c=='o'){code.push_back(c);i++;}
        else cout<<"Try again"<<endl;
   }
   
   for(int i=0;i<4;i++)
    {
        if(guess[i] == code[i]) {hints.push_back('O');  m++;}
        if(m==4) {result=true;}



*/
